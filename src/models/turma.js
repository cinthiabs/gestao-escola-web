export default class Turma {
  constructor({ id, nome, descricao, quantidadeAlunos }) {
    this.id = id;
    this.nome = nome;
    this.descricao = descricao;
    this.quantidadeAlunos = quantidadeAlunos;
  }
} 